# Tests of nonlinear_circuit.hpp

> `void circuit()` is tested against the reference solution with LinSpaced(20, 0, 20) Uin and alpha = 8, beta = 1.

> `void plotU()` is not tested. Run the program to see its output.
